# Bash scripts
create_gitignore.sh
update_readme.sh
SetUpVaultSecrets.sh

# Not necessary for the project
/vault/vault-config.hcl
/vault/
/monitoring/prometheus.yml
/monitoring/



# Go dependencies and build artifacts
/backend/*.exe
/backend/main
/backend/tmp/
/vendor/

# Docker artifacts
*.dockerignore
docker-compose.yml
*.tar.gz

# Terraform files
/terraform/.terraform/
/terraform/terraform.tfstate
/terraform/terraform.tfstate.backup
/terraform/.terraform.lock.hcl
/terraform/*.tfvars
/terraform/tfplan


# Node.js (optional, for frontend tooling)
node_modules/
npm-debug.log
package-lock.json

# AWS credentials and sensitive files
.env
*.pem
*.key
awscliv2.zip
AWSCLIV2.pkg
.aws/

# System and editor files
.DS_Store
*.swp
*.swo
.vscode/
.idea/
*.log

# Temporary files and caches
*.tmp
*.cache
*.bak
*.backup

# ============================================================
# CRITICAL: Secrets and Sensitive Files — NEVER commit these
# ============================================================
SetUpVaultSecrets.sh
vault-config.hcl
*.pem
*.key
.env
.env.*
awscliv2.zip
AWSCLIV2.pkg
.aws/

# ============================================================
# Terraform — state files contain resource IDs & may have secrets
# ============================================================
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform.lock.hcl
terraform/*.tfvars
terraform/tfplan
tfplan
*.tfstate
*.tfstate.backup

# ============================================================
# Go
# ============================================================
/backend/go.sum
/backend/*.exe
/backend/main
/backend/tmp/
/vendor/
# compiled binary (stale build artifact)
/backend/cloudpulse

# ============================================================
# Docker
# ============================================================
*.dockerignore
docker-compose.yml
*.tar.gz

# ============================================================
# Node.js (optional frontend tooling)
# ============================================================
node_modules/
npm-debug.log
package-lock.json

# ============================================================
# Bash setup scripts (not needed in repo)
# ============================================================
setup_cloudpulse_structure.sh
install_dependencies.sh
create_gitignore.sh
update_readme.sh

# ============================================================
# System and editor files
# ============================================================
.DS_Store
*.swp
*.swo
.vscode/
.idea/
*.log
*.tmp
*.cache
*.bak
*.backup
# ============================================================
# AI assistant context file (local reference, not for the repo)
# ============================================================
claude.md
